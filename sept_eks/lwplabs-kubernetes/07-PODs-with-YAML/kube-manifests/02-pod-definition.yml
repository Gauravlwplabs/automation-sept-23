apiVersion: v1 # String
kind: Pod  # String
metadata: # Dictionary
  name: myapp-pod
  namespace: myapp
  labels: # Dictionary 
    app: myapp         
spec:
  containers: # List
    - name: myapp
      image: nginx
      ports:
        - containerPort: 80


    